import { CaseIntake } from '../types';

/**
 * Historical initial cases reference container.
 * Application business data is now stored and fetched directly from Supabase PostgreSQL.
 */
export const INITIAL_CASES: CaseIntake[] = [];
